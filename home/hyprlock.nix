{...}:
{
# Hyprlock
  programs.hyprlock = {
    enable = true;
    settings = {
    # background = [{ path = "screenshot"; blur_passes = 2; }];
    # input-field = [{ size = "200, 50"; outline_thickness = 3; }];
    };
  };
}
